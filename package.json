{
  "name": "cab432-a1",
  "version": "1.0.0",
  "description": "Cloud Application ( AWS | NodeJS | ReactJS )",
  "main": "index.js",
  "scripts": {
    "project-install": "npm i && npm run client-install && npm run client-build && npm run server-install && npm run server-build",
    "client": "npm run start --prefix client",
    "server": "npm run server --prefix server",
    "client-install": "npm install --prefix client",
    "server-install": "npm install --prefix server",
    "server-test": "npm run test --prefix server",
    "client-build": "npm run build --prefix client",
    "server-build": "npm run build --prefix server",
    "dev": "concurrently 'npm run server' 'npm run client' "
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ericlkl/CAB432-A1.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Ericlkl/CAB432-A1/issues"
  },
  "homepage": "https://github.com/Ericlkl/CAB432-A1#readme",
  "devDependencies": {
    "concurrently": "^4.1.2"
  }
}
